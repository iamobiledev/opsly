{
  "name": "@opsly/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@opsly/core": "workspace:*",
    "@opsly/db": "workspace:*",
    "@opsly/integrations": "workspace:*",
    "bullmq": "^5.79.2",
    "ioredis": "^5.11.1"
  }
}
